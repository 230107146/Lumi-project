{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Debug Nest (ts-node)",
			"type": "pwa-node",
			"request": "launch",
			"program": "${workspaceFolder}/node_modules/ts-node-dev/lib/cli.js",
			"args": [ "--transpile-only", "src/main.ts" ],
			"cwd": "${workspaceFolder}",
			"console": "integratedTerminal"
		}
	]
}
